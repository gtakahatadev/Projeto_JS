// Requisitos funcionais(definem o que o sistema deve fazer (ações e regras de negócio) e não funcionais do sistema determinam como o sistema deve operar (qualidade, velocidade e segurança);

// ALGORITMO: conjunto de funções (sequência de passos, ordenada para executar uma ação em específico)

// Capturar as informações das caixas de texto e mandar para o Backend;

let formInput = document.getElementById("hero-input");
let formInputTwo = document.getElementById("cta-input");
let mainTitle = document.getElementById("hero-title");